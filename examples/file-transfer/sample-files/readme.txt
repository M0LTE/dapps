This is a sample text file shipped with the DAPPS file-transfer
example app.

When you send this file via the example, the receiver displays it
as a plain "Download" link rather than an inline preview - that's
the non-image fallback.

Try sending it to your own callsign to see the round-trip.
